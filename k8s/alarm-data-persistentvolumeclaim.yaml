apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: alarm-data
  name: alarm-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
