apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: log-data
  name: log-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
