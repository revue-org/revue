apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: notification-data
  name: notification-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
