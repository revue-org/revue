apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: revue-alarm-db
  name: revue-alarm-db
spec:
  ports:
    - name: "27019"
      port: 27019
      targetPort: 27017
  selector:
    io.kompose.service: revue-alarm-db
  type: ClusterIP
