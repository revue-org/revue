apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f all-docker-compose.yml -o k8s
    kompose.version: 1.32.0 (HEAD)
  labels:
    io.kompose.service: revue-auth-db
  name: revue-auth-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: revue-auth-db
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f all-docker-compose.yml -o k8s
        kompose.version: 1.32.0 (HEAD)
      labels:
        io.kompose.network/revue-network: "true"
        io.kompose.service: revue-auth-db
    spec:
      containers:
        - env:
            - name: MONGO_INITDB_DATABASE
              value: auth
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: example
            - name: MONGO_INITDB_ROOT_USERNAME
              value: root
          image: mongo
          name: revue-auth-db
          ports:
            - containerPort: 27017
              hostPort: 27017
              protocol: TCP
          volumeMounts:
            - mountPath: /data/db
              name: auth-data
            - mountPath: /docker-entrypoint-initdb.d/auth-init.js
              name: revue-auth-db-claim1
      restartPolicy: Always
      volumes:
        - name: auth-data
          persistentVolumeClaim:
            claimName: auth-data
        - name: revue-auth-db-claim1
          persistentVolumeClaim:
            claimName: revue-auth-db-claim1
