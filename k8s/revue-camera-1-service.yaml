apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: revue-camera-1
  name: revue-camera-1
spec:
  ports:
    - name: "5001"
      port: 5001
      targetPort: 5001
  selector:
    io.kompose.service: revue-camera-1
  type: ClusterIP
