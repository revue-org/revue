apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: revue-location-data
  name: revue-location-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
