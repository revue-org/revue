apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: revue-log-data
  name: revue-log-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
