apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: revue-log-db
  name: revue-log-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: revue-log-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: revue-log-db
    spec:
      containers:
        - env:
            - name: MONGO_INITDB_DATABASE
              value: log
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: example
            - name: MONGO_INITDB_ROOT_USERNAME
              value: root
          image: mongo
          name: revue-log-db
          ports:
            - containerPort: 27017
              protocol: TCP
          volumeMounts:
            - mountPath: /data/db
              name: revue-log-data
            - mountPath: /docker-entrypoint-initdb.d/history-init.js
              name: revue-log-db-cm1
              subPath: history-init.js
      restartPolicy: Always
      volumes:
        - name: revue-log-data
          persistentVolumeClaim:
            claimName: revue-log-data
        - configMap:
            items:
              - key: history-init.js
                path: history-init.js
            name: revue-log-db-cm1
          name: revue-log-db-cm1
